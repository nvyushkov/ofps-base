%h1 Listing ipvs

%table
  %thead
    %tr
      %th
      %th
      %th
      
  %tbody
    - @ipvs.each do |ipv|
      %tr
        %td= link_to 'Show', ipv
        %td= link_to 'Edit', edit_ipv_path(ipv)
        %td= link_to 'Destroy', ipv, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Ipv', new_ipv_path
